# 루프벤트 SEO 설계 메모

「구 이름 + 옥상 환풍기 교체 / 설치」 검색에서 노출되는 것이 목표입니다.
어떤 판단으로 이렇게 만들었는지 정리해 둡니다.

---

## 1. 키워드 → 페이지 배치

| 검색어 유형 | 예시 | 받는 페이지 |
|---|---|---|
| 지역 + 서비스 | 강서구 옥상 환풍기 교체, 남동구 벤츄레이터 설치 | `/vent/regions/{slug}/` (36개) |
| 서비스 단독 | 옥상 환풍기 교체, 무동력 벤츄레이터 설치 | `/` · `/vent/` |
| 규격 | 300파이 벤츄레이터, 벤츄레이터 규격 | `/vent/spec/` · `/vent/guides/size/` |
| 증상·정보 | 벤츄레이터 소음, 옥상 환풍기 안 돌아감, 좌대 부식 | `/vent/guides/{slug}/` (10개) |

**한 페이지 = 한 검색의도** 원칙으로 나눴습니다.
지역 페이지가 서비스 설명까지 다 하려 들면 어느 쪽으로도 안 잡힙니다.

---

## 2. 제목(title) 규칙

지역 페이지는 전부 이 형태로 고정했습니다.

```
{구 이름} 옥상 환풍기 교체·벤츄레이터 설치 | 루프벤트
```

- 검색어를 앞쪽에 두었습니다. 네이버는 제목 앞부분 가중치가 큽니다.
- 「옥상 환풍기」와 「벤츄레이터」를 한 제목에 같이 넣었습니다. 두 단어로 다 검색되기 때문입니다.
- 51개 페이지 title·description이 **전부 서로 다릅니다.** (빌드 시 중복 검사 통과)

---

## 3. 유사문서 회피 — 이 사이트에서 가장 신경 쓴 부분

지역 페이지 36개는 같은 서비스를 설명하기 때문에 그냥 쓰면 전부 유사문서로 걸립니다.
그래서 이렇게 처리했습니다.

- **소제목 144개가 전부 다릅니다.** (36지역 × 4소제목, 중복 0)
- **25자 이상 문장 중복 0건.** 빌드 전에 기계적으로 검사했습니다.
- 지역마다 실제 동 이름과 건물 성격을 다르게 넣었습니다.
  - 구로·금천·영등포·남동·오정 → 공장·창고 지붕
  - 노원·도봉·목동·송파 → 대단지 구축 아파트
  - 화곡·신림·계양·성북 → 빌라·다세대 밀집, 옥상 접근 문제
  - 인천 중구·연수 → 해풍 부식, 재질 선택
  - 종로·중구·동대문 → 시장·도매상가, 좁은 골목 자재 반입

**앞으로 내용을 고치실 때도 이 원칙만 지켜주세요.**
다른 지역 문장을 복사해 지역명만 바꾸면 그 순간 두 페이지가 같이 죽습니다.

---

## 4. 내부 링크

```
메인 ─┬─ /vent/ (허브) ─┬─ 지역 36
      │                 ├─ 가이드 10
      │                 └─ /vent/spec/
      ├─ /cases/
      └─ /contact/
```

- 지역 페이지 → 가이드 2~4개 (지역마다 링크 조합을 다르게 분산)
- 가이드 → 다른 가이드 3개 + 지역 칩 전체
- 모든 페이지 하단에 지역 칩 36개 → 새 지역 페이지도 빨리 발견됩니다
- 빵부스러기(breadcrumb) 구조화데이터 적용

---

## 5. 구조화 데이터 (JSON-LD)

| 종류 | 들어간 페이지 | 효과 |
|---|---|---|
| `HomeAndConstructionBusiness` | 전 페이지 | 업체·전화번호·시공지역 인식 |
| `Service` | 메인·허브·규격·지역 | 서비스 종류와 지역 연결 |
| `FAQPage` | 메인·허브·규격·지역 36·가이드 10 | 구글 FAQ 리치결과 노출 가능 |
| `Article` | 가이드 10 | 정보성 글로 인식 |
| `BreadcrumbList` | 하위 전 페이지 | 검색결과에 경로 표시 |

---

## 6. 기술적 처리

- 정적 HTML — 자바스크립트 없이 본문이 그대로 있어 네이버 Yeti 봇이 확실히 읽습니다.
- `robots.txt` 에 Yeti(네이버) 명시 허용
- 모든 페이지 canonical, og 태그, 모바일 대응
- 사진은 webp + `loading="lazy"` + `width/height` 지정 (레이아웃 흔들림 방지)
- 외부 스크립트 없음 → 로딩 빠름

---

## 7. 오픈 후 순서

**1주차**
- 소유확인 코드 넣고 재배포 → 사이트맵 제출(네이버·구글)
- `dist/urls.txt` 로 수집요청. 메인 → 허브 → 지역 순
- 네이버 플레이스(스마트플레이스) 등록. 지역 검색에서 홈페이지와 함께 뜹니다

**2~4주차**
- 시공할 때마다 전·후 사진을 찍어 지역별로 사례 추가 (사진이 곧 순위입니다)
- 네이버 블로그가 있다면 지역 시공기를 쓰고 해당 지역 페이지로 링크

**2차 오픈 (경기 확대)**
- 고양·성남·수원·용인·안양·의정부·김포·광명·시흥·안산·남양주·하남·구리·파주 등
- `regions-7.json` 을 만들어 같은 방식으로 추가하면 됩니다

---

## 8. 하지 말아야 할 것

- 같은 문장을 여러 지역에 복붙 (유사문서)
- 한 페이지에 검색어를 억지로 반복 (키워드 스터핑)
- 없는 시공 사례를 지어내거나 다른 업체 사진 사용
- 가격을 페이지에 박아두기 — 지금은 일부러 한 군데도 넣지 않았습니다.
  넣으실 거면 「비용을 좌우하는 요소」 페이지에만 범위로 적는 편이 안전합니다.
